#pragma once

#include <cstdint>

namespace sws
{
	/**
	 * \brief The type used to define the length of a \c sws::Packet
	 */
	using packetlen_t = uint16_t;

	/**
	 * \brief Type used to define the port of an address.
	 */
	using port_t = uint16_t;
}
